function sumOfElements(input) {
  let totalSum = 0;
  for (let i = 0; i < input.length; i++) {
    totalSum = totalSum+ input[i];
  }

  return totalSum;
}
let result=sumOfElements([1,2,2]);
console.log(result);

function averageOfArray(b-'''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''){
   return sumOfElements(list)/list.length;
   
   }
let listAverage=averageOfArray([1,2,3]);
console.log(listAverage);

